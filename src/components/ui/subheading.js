function Subheading({ children }) {
  return (
    <span className="mb-4 block text-left text-base font-medium uppercase tracking-[0.75px] text-burnt-orange-200">
      {children}
    </span>
  );
}

export default Subheading;
